import { createApp } from 'vue'
import './index.css'
import App from './App.vue'
// If you want to use AppNew, simply import it instead or map it
// import AppNew from './AppNew.vue'

createApp(App).mount('#app')
